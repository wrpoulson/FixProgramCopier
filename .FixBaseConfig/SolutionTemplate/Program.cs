using FixBase.Implementations;
using FixBase.Implementations.Interfaces;

namespace FixBase
{
  class Program
  {
    static void Main(string[] args)
    {
      //CompositionRoot.Wire(new ApplicationModule());
      //var fixer = CompositionRoot.Resolve<IFix>();
      //fixer.Fix();
    }
  }
}
